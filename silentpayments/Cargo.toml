[package]
name = "silentpayments"
version.workspace = true
authors.workspace = true
license.workspace = true
repository = "https://github.com/cygnet3/spdk/tree/master/silentpayments"
homepage = "https://github.com/cygnet3/spdk/tree/master/silentpayments"
edition.workspace = true
description = "A library for adding BIP352: Silent Payments support to wallets."
keywords = ["bitcoin", "secp256k1"]
readme = "README.md"
exclude = ["tests/resources"]

[lib]
name = "silentpayments"
crate-type = ["lib"]

[features]
default = ["encode", "sending", "receiving", "dleq-standalone"]
encode = ["dep:bech32"]
serde = ["dep:serde"]
sending = ["dep:bitcoin_hashes", "dep:hex", "encode"]
receiving = ["dep:bitcoin_hashes", "dep:hex", "serde", "encode"]
dleq-standalone = ["sending", "dep:rust-dleq", "rust-dleq/standalone"]
dleq-native = ["sending", "dep:rust-dleq", "rust-dleq/native"]

[dependencies]
secp256k1.workspace = true
hex = { workspace = true, optional = true }
bech32 = { workspace = true, optional = true }
serde = { workspace = true, optional = true }
bitcoin_hashes = { workspace = true, optional = true }
rust-dleq = { workspace = true, optional = true }

[dev-dependencies]
bip39 = { version = "2.2", features = ["rand"] }
bitcoin = { version = "0.32.102", features = ["serde"] }
serde_json = "1.0"

[[example]]
name = "create_wallet"
required-features = ["receiving"]

[[example]]
name = "find_output"
required-features = ["receiving"]

[[test]]
name = "vector_tests"
required-features = ["receiving", "sending", "dleq-standalone"]
